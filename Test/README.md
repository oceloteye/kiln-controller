Automated modal close test

Requirements:
- Node.js (16+ recommended)

Install and run (from repository root):

```bash
cd Test
npm install
npm run modal:e2e
```

The script will exit with code 0 on success, non-zero on failure. On failure it writes `Test/modal_e2e_failure.png` to help diagnose the problem.
