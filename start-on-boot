#!/bin/bash
# Determine script directory so the script works from any user directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
sudo cp "$SCRIPT_DIR/lib/init/kiln-controller.service" /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable kiln-controller
